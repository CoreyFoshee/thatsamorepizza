<!-- Catering-Specific Navigation Bar -->
<nav class="bg-white shadow-lg sticky top-0 z-50 border-b-2 border-primary/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex-shrink-0">
                <a href="/" class="flex items-center">
                    <img src="/Logo/TA_LOGO_Final_black.png" alt="That's Amore Catering Logo" class="h-12 w-auto">
                    <div class="ml-3">
                        <div class="text-xl font-heading font-bold text-primary">That's Amore</div>
                        <div class="text-sm text-gray-600">Catering</div>
                    </div>
                </a>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:block">
                <div class="ml-10 flex items-baseline space-x-8">
                    <a href="/" class="<%= currentPage === 'home' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                        Home
                    </a>
                    <a href="/menu" class="<%= currentPage === 'menu' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                        Menu
                    </a>
                    <a href="/services" class="<%= currentPage === 'services' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                        Services
                    </a>
                    <a href="/quote" class="<%= currentPage === 'quote' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                        Get Quote
                    </a>
                    <a href="/contact" class="<%= currentPage === 'contact' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                        Contact
                    </a>
                </div>
            </div>

            <!-- CTA Button -->
            <div class="hidden md:block">
                <a href="tel:504-463-5384" class="bg-primary text-white px-6 py-2 rounded-full text-sm font-medium hover:bg-primary/90 transition-all duration-300 transform hover:scale-105">
                    Call (504) 463-5384
                </a>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden">
                <button type="button" class="mobile-menu-button bg-white text-gray-700 hover:text-primary focus:outline-none focus:text-primary transition-colors duration-200">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile Navigation Menu -->
    <div class="mobile-menu hidden md:hidden bg-white border-t border-gray-200">
        <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <a href="/" class="<%= currentPage === 'home' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> block px-3 py-2 rounded-md text-base font-medium">
                Home
            </a>
            <a href="/menu" class="<%= currentPage === 'menu' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> block px-3 py-2 rounded-md text-base font-medium">
                Menu
            </a>
            <a href="/services" class="<%= currentPage === 'services' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> block px-3 py-2 rounded-md text-base font-medium">
                Services
            </a>
            <a href="/quote" class="<%= currentPage === 'quote' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> block px-3 py-2 rounded-md text-base font-medium">
                Get Quote
            </a>
            <a href="/contact" class="<%= currentPage === 'contact' ? 'bg-primary text-white' : 'text-gray-700 hover:text-primary' %> block px-3 py-2 rounded-md text-base font-medium">
                Contact
            </a>
            <div class="pt-4">
                <a href="tel:504-463-5384" class="bg-primary text-white block w-full text-center px-6 py-3 rounded-full text-base font-medium hover:bg-primary/90 transition-all duration-300">
                    Call (504) 463-5384
                </a>
            </div>
        </div>
    </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    const mobileMenu = document.querySelector('.mobile-menu');

    mobileMenuButton.addEventListener('click', function() {
        mobileMenu.classList.toggle('hidden');
    });

    // Close mobile menu when clicking on a link
    const mobileMenuLinks = mobileMenu.querySelectorAll('a');
    mobileMenuLinks.forEach(link => {
        link.addEventListener('click', function() {
            mobileMenu.classList.add('hidden');
        });
    });
});
</script>
